import { Button, } from 'react-native';

export default function SingleButton({ press }) {
    
    return  <Button
                title='aceptar'
                onPress={press}>
            </Button>
}
